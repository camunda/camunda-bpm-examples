<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.camunda.bpm.quickstart</groupId>
  <artifactId>root</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Camunda Examples Root</name>

  <properties>
    <!-- CAMUNDA -->
    <version.camunda>7.22.0</version.camunda>
    <version.camunda.ee>7.22.0-ee</version.camunda.ee>

    <!-- DEPENDENCIES -->
    <version.h2>2.3.232</version.h2>
    <version.groovy>4.0.22</version.groovy>
    <version.graaljs>21.1.0</version.graaljs>
    <version.node>v18.16.0</version.node>
    <version.npm>10.7.0</version.npm>
    <version.logback>1.2.11</version.logback>
    <version.slf4j>1.7.26</version.slf4j>
    <version.jackson>2.15.2</version.jackson>
    <version.jaxb>4.0.5</version.jaxb>
    <version.resteasy3>3.15.3.Final</version.resteasy3>
    <version.resteasy>6.2.3.Final</version.resteasy>
    <version.jakartaee>10.0.0</version.jakartaee>
    <version.java-ee6>3.0.3.Final</version.java-ee6>
    <version.java-ee>7.0</version.java-ee>
    <version.jboss-javaee>3.0.3.Final</version.jboss-javaee>
    <version.jakarta-servlet-api>6.0.0</version.jakarta-servlet-api>
    <version.servlet-api>4.0.1</version.servlet-api>
    <version.quarkus>3.15.0</version.quarkus>
    <version.spring>6.1.8</version.spring>
    <version.spring5>5.3.36</version.spring5>
    <version.spring.boot>3.3.3</version.spring.boot>
    <version.tomcat>10.1.30</version.tomcat>
    <version.wildfly26>26.1.3.Final</version.wildfly26>
    <version.wildfly-arquillian-container-managed>2.2.0.Final</version.wildfly-arquillian-container-managed>
    <version.wildfly-arquillian-container-remote>3.0.1.Final</version.wildfly-arquillian-container-remote>
    <version.template-engine-xslt>2.2.0</version.template-engine-xslt>
    <version.twitter4j>4.0.7</version.twitter4j>

    <!-- TESTING -->
    <version.junit>4.13.2</version.junit>
    <version.junit5>5.9.3</version.junit5>
    <version.assertj>3.26.3</version.assertj>
    <version.mockito>4.4.0</version.mockito>
    <version.commons-lang>3.17.0</version.commons-lang>
    <version.arquillian>1.1.10.Final</version.arquillian>

    <!-- MAVEN -->
    <version.maven-compiler-plugin>3.13.0</version.maven-compiler-plugin>
    <version.surefire-plugin>3.1.2</version.surefire-plugin>
    <version.frontend-maven-plugin>1.14.0</version.frontend-maven-plugin>
    <version.cargo-maven-plugin>1.10.9</version.cargo-maven-plugin>
    <version.maven-dependency-plugin >2.8</version.maven-dependency-plugin>
    <version.maven-resources-plugin>3.3.1</version.maven-resources-plugin>
    <version.maven-war-plugin>3.3.2</version.maven-war-plugin>
    <version.maven-war-plugin>3.4.0</version.maven-war-plugin>


    <internal.version-bump.include>**/pom.xml</internal.version-bump.include>
    <internal.version-bump.exclude>/internal/**</internal.version-bump.exclude>
    <internal.version-bump.dir>../..</internal.version-bump.dir>
  </properties>

  <profiles>
    <profile>
      <id>bump-versions</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <phase>package</phase>
                <configuration>
                  <target>
                    <!-- CAMUNDA VERSION -->
                    <replaceregexp byline="true">
                      <regexp pattern="(&lt;version.camunda&gt;).*(&lt;/version.camunda&gt;)"/>
                      <substitution expression="\1${version.camunda}\2"/>
                      <fileset dir="${internal.version-bump.dir}">
                        <include name="${internal.version-bump.include}"/>
                        <exclude name="${internal.version-bump.exclude}"/>
                      </fileset>
                    </replaceregexp>

                    <!-- JUNIT VERSION -->
                    <replaceregexp byline="true">
                      <regexp pattern="(&lt;version.junit&gt;).*(&lt;/version.junit&gt;)"/>
                      <substitution expression="\1${version.junit}\2"/>
                      <fileset dir="${internal.version-bump.dir}">
                        <include name="${internal.version-bump.include}"/>
                        <exclude name="${internal.version-bump.exclude}"/>
                      </fileset>
                    </replaceregexp>

                    <!-- H2 VERSION -->
                    <replaceregexp byline="true">
                      <regexp pattern="(&lt;version.h2&gt;).*(&lt;/version.h2&gt;)"/>
                      <substitution expression="\1${version.h2}\2"/>
                      <fileset dir="${internal.version-bump.dir}">
                        <include name="${internal.version-bump.include}"/>
                        <exclude name="${internal.version-bump.exclude}"/>
                      </fileset>
                    </replaceregexp>

                    <!-- SLF4J VERSION -->
                    <replaceregexp byline="true">
                      <regexp pattern="(&lt;version.slf4j&gt;).*(&lt;/version.slf4j&gt;)"/>
                      <substitution expression="\1${version.slf4j}\2"/>
                      <fileset dir="${internal.version-bump.dir}">
                        <include name="${internal.version-bump.include}"/>
                        <exclude name="${internal.version-bump.exclude}"/>
                      </fileset>
                    </replaceregexp>

                    <!-- JAVA VERSION -->
                    <replaceregexp byline="true">
                      <regexp pattern="(&lt;maven.compiler.release&gt;).*(&lt;/maven.compiler.release&gt;)"/>
                      <substitution expression="\1${maven.compiler.release}\2"/>
                      <fileset dir="${internal.version-bump.dir}">
                        <include name="${internal.version-bump.include}"/>
                        <exclude name="${internal.version-bump.exclude}"/>
                      </fileset>
                    </replaceregexp>

                    <!-- MAVEN COMPILER VERSION -->
                    <replaceregexp byline="true">
                      <regexp pattern="(&lt;maven.compiler.version&gt;).*(&lt;/maven.compiler.version&gt;)"/>
                      <substitution expression="\1${maven.compiler.version}\2"/>
                      <fileset dir="${internal.version-bump.dir}">
                        <include name="${internal.version-bump.include}"/>
                        <exclude name="${internal.version-bump.exclude}"/>
                      </fileset>
                    </replaceregexp>
                  </target>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>


</project>
